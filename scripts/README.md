# Скрипты генерации женских версий архетипов Бацзы

## 🎯 ДВА РЕЖИМА РАБОТЫ

### 📋 Режим 1: На основе правил (РЕКОМЕНДУЕТСЯ, БЕЗ API)
**Не требует внешних API ключей!** Использует детальные правила преобразования.

```bash
cd cosmos-of-hopes-cn
node scripts/generate-female-archetypes.js
```

**Преимущества:**
- ✅ Не требует API ключей
- ✅ Быстро (несколько секунд)
- ✅ Бесплатно
- ✅ Полный контроль над преобразованием
- ✅ Работает офлайн

**Как работает:**
- Применяет детальные правила преобразования метафор, глаголов, понятий
- Учитывает тип контента (forecast, energy, advice, ritual, transformation)
- Учитывает элемент (Дерево, Огонь, Земля, Металл, Вода)
- Создает естественные женские версии

### 🤖 Режим 2: Через AI (опционально)
Требует API ключ (OpenAI или DeepSeek).

```bash
# OpenAI
export OPENAI_API_KEY=sk-...
node scripts/generate-female-archetypes.js --ai

# DeepSeek (пока не реализован)
export DEEPSEEK_API_KEY=...
node scripts/generate-female-archetypes.js --deepseek
```

## 📋 ИСПОЛЬЗОВАНИЕ

### Шаг 1: Экспорт мужских архетипов

```bash
cd cosmos-of-hopes-cn
node scripts/export-male-archetypes.js
```

Создаст файл `data/male-archetypes-export.json` со всеми текущими мужскими текстами.

### Шаг 2: Генерация женских версий

**Рекомендуемый способ (правила):**
```bash
node scripts/generate-female-archetypes.js
```

**Или через AI (если есть ключ):**
```bash
node scripts/generate-female-archetypes.js --ai
```

**Результат:**
- Файл `data/female-archetypes-generated.json`
- 1500 записей (150 архетипов × 5 типов × 2 стиля)
- Каждая запись содержит `male` и `female` версии

### Шаг 3: Проверка результатов

После генерации проверьте файл `data/female-archetypes-generated.json`:
- Должно быть 1500 записей
- Каждая запись содержит `male` и `female` версии
- Проверьте качество на выборке

## 🔧 СТРУКТУРА

```
scripts/
├── export-male-archetypes.js          # Экспорт текущих мужских архетипов
├── generate-female-archetypes.js      # Генерация (поддерживает 2 режима)
├── rules-based-female-generator.js    # Правила преобразования (БЕЗ API)
└── config/
    └── openai-config.js               # Конфигурация OpenAI (для AI режима)
```

## 📊 ПРАВИЛА ПРЕОБРАЗОВАНИЯ

Система правил включает:

1. **Метафоры** - замена образов (дуб → ива, меч → зеркало)
2. **Глаголы действия** - изменение формулировок (прояви → укрепи проявление)
3. **Понятия** - замена концепций (воин → хранительница, битва → путешествие)
4. **Время и ритмы** - изменение временных акцентов (утром → вечером)
5. **Фокусы** - изменение приоритетов (экспансия → углубление)

Правила применяются с учетом:
- Типа контента (forecast, energy, advice, ritual, transformation)
- Элемента (Дерево, Огонь, Земля, Металл, Вода)
- Уровня силы (weak, medium, strong)

## 🎯 РЕКОМЕНДАЦИЯ

**Используйте режим правил** - он:
- ✅ Не требует внешних API
- ✅ Работает быстро и надежно
- ✅ Дает хорошие результаты
- ✅ Можно легко доработать правила вручную

**AI режим** используйте только если:
- Есть API ключ
- Нужна более креативная генерация
- Готовы к дополнительным расходам

## 📝 СЛЕДУЮЩИЕ ШАГИ

После генерации:
1. Ручная проверка критических архетипов (20%)
2. Интеграция в `content-generator.js`
3. Тестирование различий мужских/женских версий
